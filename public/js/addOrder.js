const addOrder = function(e){

     //Avoid reset
     e.preventDefault();

     // Get the stock information
     const stockTag = $('tbody tr');

     console.log("Ordertable", stockTag);

     const newOrder = [];

}